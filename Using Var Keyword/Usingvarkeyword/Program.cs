namespace Usingvarkeyword
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // The var keyword assigns the most appropriate datatype to a variable input, it must be initialised
            // var variables also cannot be null

            var number = 0; // integer
            var text = "Text"; // string
            var isTrue = false; // boolean
        }
    }
}